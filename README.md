# FetchAnApiWithJsonData
Fetch the data form an json api with the help of JSONObject.
we take the url and open connection with .openstream() method, store into inputstream reference.
make object of buffer reader and also make object InputStreamReader() then pass inputstream.
Then make readdata method, append the data into StringBuilder().
return sequence of character.
Store in JSONObject.
